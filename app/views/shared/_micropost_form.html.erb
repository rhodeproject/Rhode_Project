<%= form_for(@micropost) do |f| %>
    <%= render 'shared/micropost_error', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Compose admin message..." %>
    </div>
    <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>